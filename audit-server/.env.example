# amz-cli 审计接收端配置模板(复制为 .env 或在 Zeabur 环境变量里设置;不要提交真实值)
PORT=8080

# 上报密钥:CLI 端的 AMZ_AUDIT_TOKEN 必须与此完全一致。换成一个长随机串。
AUDIT_TOKEN=

# 看板登录密码(管理员)。
DASHBOARD_PASSWORD=

# 数据目录(部署时挂持久卷)。
AUDIT_DATA_DIR=/data

# 会话签名密钥(建议单独设一个随机串;留空则由密码派生)。
SESSION_SECRET=
